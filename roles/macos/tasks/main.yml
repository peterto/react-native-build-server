---
- name: Get stats on macOS
  ansible.builtin.debug:
    var: ansible_facts

- name: Print information on remote host
  ansible.builtin.debug:
    var: hostvars[inventory_hostname]
    verbosity: 4
